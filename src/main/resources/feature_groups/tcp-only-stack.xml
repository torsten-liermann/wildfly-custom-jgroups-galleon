<?xml version="1.0" encoding="UTF-8"?>
<feature-group-spec xmlns="urn:jboss:galleon:feature-group:1.0" 
                    name="tcp-only-stack">
    
    <!-- 
        Attempt to create TCP-only configuration by excluding UDP components
        from the wildfly-ee feature pack we depend on
    -->
    
    <!-- 
        This demonstrates the limitation: we cannot reference or modify 
        feature groups from the dependency feature pack (wildfly-ee)
    -->
    
</feature-group-spec>